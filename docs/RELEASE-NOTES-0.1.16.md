## 0.1.16 — 2026-09-17

- Added an **All academic years** option to the Windows GUI and interactive CLI wizard.
- An unrestricted filter is stored as an empty academic-year filter and therefore does not exclude courses during discovery or synchronization.
- CLI flags accept `all`, `any`, or `*` for the unrestricted mode.
